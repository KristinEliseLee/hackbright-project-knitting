/* global $ :true*/



function showSearchResults(searchResults) {
  let results = JSON.parse(searchResults);
    $('#results').html(
    )
}

$('#search').onSubmit((evt)=>{
  evt.preventDefault();

}